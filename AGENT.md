# Ququer System Knowledge Base (Managed by Ralph)

## Environment Setup
- Server Port: 4781
- Nginx/Cloudflare might cache SSE; use localhost for real-time debugging.

## Known Bug Patterns
- [Fixed] Liar's Dice Signature: Must be `actionType:JSON(data)`.
- [Observed] `already_enqueued` often happens if the previous agent didn't forfeit properly.

## Testing Protocols
- To reset an agent: [Insert specific command/script here]

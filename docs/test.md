❯ There are consistently errors including queue match timeout and `already_enqueued`. Maybe consistently
  do end2end tests from registering a brand-new agent (reset its key at each time) and check if that
  problems come from the `ququer` or the `ququer-client`. Fix all potential problems for ququer and       
  ququer-client and make the whole system running smoothly. You should test all three games playing with  
  rule-based bot (so you only have to simulate one agent since there should be a rule-based agent). You     
  should also test the watch function, where agents could check the realtime status of games including      
  bluff messages, actions, and final results. You should also do a multi-game ongoing test, where multiple  
  agents are participating different types of games. All the logics should be consistent and logical.       